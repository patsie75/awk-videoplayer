#!/usr/bin/gawk -f
@include "src/decfnc.gawk"

BEGIN {
  RS = ".{" width * bytes_per_pix "}"
}

{
  byte = 1
  line = ""
  split(RT, data, "")
  
  for (x=0; x<width; x++)
  {
    line = line @decfnc(data, byte) " "
    byte += bytes_per_pix
  }

  printf("%s\n", line)
  fflush()
}

